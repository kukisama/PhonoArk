<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PhonoArk.Mobile.App.Pages.PhonemeDetailPage"
             Title="音标详情"
             BackgroundColor="{StaticResource BackgroundColor}">

    <ScrollView>
        <VerticalStackLayout Spacing="16" Padding="24,16">

            <!-- 音标符号大字显示 -->
            <Frame BackgroundColor="{StaticResource CardColor}"
                   CornerRadius="16"
                   Padding="24"
                   HasShadow="True"
                   BorderColor="Transparent">
                <VerticalStackLayout Spacing="8">
                    <Label x:Name="LblSymbol"
                           Style="{StaticResource PhonemeSymbolLarge}" />
                    <Label x:Name="LblDescription"
                           FontSize="16"
                           HorizontalTextAlignment="Center"
                           TextColor="{StaticResource TextSecondaryColor}" />
                </VerticalStackLayout>
            </Frame>

            <!-- 标题 -->
            <Label Text="示例单词"
                   FontSize="20"
                   FontAttributes="Bold"
                   TextColor="{StaticResource TextPrimaryColor}"
                   Margin="0,8,0,0" />

            <!-- 单词列表 -->
            <CollectionView x:Name="WordList">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="{StaticResource CardColor}"
                               CornerRadius="12"
                               Padding="16"
                               HasShadow="True"
                               BorderColor="Transparent"
                               Margin="0,4">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnWordClicked" />
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                                <!-- 单词 -->
                                <Label Text="{Binding Word}"
                                       FontSize="22"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource TextPrimaryColor}"
                                       Grid.Row="0" Grid.Column="0" />

                                <!-- 发音按钮 -->
                                <Label Text="🔊"
                                       FontSize="24"
                                       VerticalTextAlignment="Center"
                                       Grid.Row="0" Grid.Column="1"
                                       Grid.RowSpan="2" />

                                <!-- 音标标注 -->
                                <Label Text="{Binding IpaTranscription}"
                                       FontSize="16"
                                       TextColor="{StaticResource SecondaryColor}"
                                       Grid.Row="1" Grid.Column="0" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
